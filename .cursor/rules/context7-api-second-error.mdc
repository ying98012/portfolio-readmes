---
description: API 二次編譯錯誤時強制查 Context7
alwaysApply: true
---

# Context7 觸發條件（API 編譯錯誤）

當同一實作任務中，API 相關編譯錯誤出現第 2 次時，必須先查 `context7` MCP，再繼續修改程式。

## 觸發判定

- 第 1 次 API 編譯錯誤：可先依現有上下文修正一次。
- 第 2 次仍為 API 編譯錯誤（同套件/同功能域）：立即觸發本規則。

## 強制流程

1. 先確認專案實際使用的套件版本（`package.json`、`Gemfile.lock`、`requirements.txt` 等）。
2. 使用 `context7` MCP 查該版本官方文件與 API 範例。
3. 以查到的版本相容 API 重寫或調整實作。
4. 重新編譯/測試後才可進行下一步。

## 實作要求

- 禁止在第 2 次 API 編譯錯誤後繼續靠猜測修改。
- 回覆中需簡短說明：查了哪個版本、替換了哪個 API。
